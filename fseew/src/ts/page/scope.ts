import SEARCH from '@fluentui/svg-icons/icons/search_sparkle_20_color.svg'

export function entry(element: HTMLElement) {
    const header: Element = element.nth(1);

    const fluentTextInput: Element = header.nth(1);
    // fluentTextInput.nth(1).innerHTML = SEARCH;
}