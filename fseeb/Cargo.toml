cargo-features = ["trim-paths"]

[workspace]
resolver = "2"
members = [
    "fseec",
    "fsees",
    "fseeu"
]

[profile.release]
lto = true
strip = true
debug = false
panic = "abort"
opt-level = "z"
trim-paths = "all"
incremental = false
codegen-units = 1